# AI Handoff — ЭТП (электронная торговая площадка с ИИ)

Этот файл — контекст для продолжения работы в новой сессии/проекте («ETP X» в интерфейсе Claude). Прочитай его целиком перед тем, как что-то менять в коде.

## Что это за проект

Полноценная ЭТП с ИИ-помощником — не то же самое, что соседний проект `specification` (там простой статичный `index.html` со страницей спецификации Заказчик/Участник). Этот проект (`/Users/emilakmaev/Downloads/etp`) построен с нуля по `PROJECT_BRIEF.md` и `schema.prisma`, которые пользователь уже спроектировал сам до начала работы.

Пользователь **не разработчик** — для любых шагов, которые требуют от него что-то сделать руками (кликнуть в чужой панели, найти ключ, вставить пароль), давай короткие пронумерованные шаги простым языком, без жаргона без пояснения.

## Стек

Next.js 16 (App Router) + TypeScript + Tailwind v4 + shadcn/ui (new-york, база — **Base UI**, не Radix) + Prisma 6.19.3 + Postgres (Supabase) + Supabase Auth + pnpm. Хостинг предполагается Vercel (не GitHub Pages — там не будет работать серверная часть). AI-фичи (Anthropic API) пока не подключены.

## Текущий статус (на 2026-08-03)

Всё ниже проверено вживую в браузере, не только `pnpm build`:

- Регистрация/вход через Supabase Auth (email+пароль), таблицы Prisma созданы в реальной базе (`prisma db push` уже выполнялся)
- Онбординг компании (создание, первый участник → ADMIN); присоединение к существующей компании не реализовано — это v2 по брифу
- Дашборд, `/procedures/new`, `/procedures/[id]`, `/inbox` — пока страницы-заглушки с реальной защитой доступа (`requireCompany()`)
- Полноценный sidebar-шелл (shadcn `sidebar-07`, адаптирован под реальные данные компании/пользователя) для дашборда/процедур/инбокса; онбординг — отдельный простой layout без сайдбара (у пользователя ещё нет компании)
- **Переключатель компаний**: пользователь может состоять в нескольких компаниях (`CompanyMember` many-to-many) — в шапке сайдбара дропдаун со списком всех компаний, галочка справа на активной (серый фон карточки), выбор хранится в cookie `active_company_id` (см. `lib/auth.ts` → `requireCompany()`, `app/(platform)/actions.ts` → `setActiveCompany`). Внизу списка — пункт **«+ Создать компанию»**, открывает модалку (Название + ИНН) поверх дашборда; создаёт компанию через `createAdditionalCompany` (как `createCompany`, но без блокировки "уже есть компания" — она для онбординга первой компании, эта для последующих) и сразу делает её активной. Дизайн дропдауна и модалки — точно по референсам пользователя (скриншоты shadcn-стиля), сверен визуально.
- Экраны входа/регистрации — shadcn `login-02`, двухколоночный лейаут, адаптирован под реальные server actions
- Шрифт — Inter (кириллица + латиница), уже пофикшена серьёзная опечатка с serif-фолбэком (см. ниже)
- Установлен project-level Claude skill `shadcn` (`.claude/skills/shadcn` → симлинк на `.agents/skills/shadcn`) — даёт агенту контекст по компонентам, документацию, поиск в реестре. Должен подхватиться автоматически в новой сессии, открытой в этой папке.
- **Задеплоено и живёт**: репозиторий на GitHub — [github.com/akmaevtat-lang/etp](https://github.com/akmaevtat-lang/etp) (публичный), продакшн — **https://etp-one.vercel.app** (Vercel-проект `emils-projects-8ad794c6/etp`). Автодеплой на каждый пуш в `main` уже настроен и работает.
- **Форма создания процедуры + таблица спецификации** реализованы и проверены вживую в браузере (создание процедуры → добавление лота → живой пересчёт цены с НДС/суммы → автосохранение на blur → удаление лота → всё переживает перезагрузку страницы):
  - `/procedures/new` — форма (тип «Закупка/Продажа» через shadcn `Select`, название, описание) → создаёт `Procedure` в статусе `DRAFT` → редирект на `/procedures/[id]`.
  - `/procedures/[id]` — шапка с типом/статусом процедуры + карточка «Спецификация» с таблицей `SpecificationItem` (номер лота, наименование, кол-во, ед. изм., ставка НДС, цена без/с НДС, сумма без/с НДС, характеристики, условия поставки). Таблица редактируемая, только пока `status === 'DRAFT'` и активная компания — организатор; иначе рендерится read-only (текст, не disabled-инпуты — тот же принцип, что и в соседнем прототипе `specification`).
  - Server actions в `app/(platform)/(dashboard)/procedures/actions.ts`: `createProcedure`, `addSpecificationItem`, `updateSpecificationItem` (пересчитывает `priceWithVat`/`totalNoVat`/`totalWithVat` на сервере — клиент не единственный источник правды), `deleteSpecificationItem`. Все проверяют, что процедура/позиция принадлежит активной компании.
  - Клиентский компонент `components/procedure/specification-table.tsx`: правки полей — сразу в локальный стейт (мгновенный пересчёт для отображения) + сохранение на `onBlur` (текстовые/числовые поля) или сразу на `onValueChange` (select ставки НДС). Добавление/удаление строки — сразу, через `useTransition`.
  - Дашборд (`/dashboard`) теперь показывает реальный список процедур компании (раньше был захардкожен пустой список) — иначе после создания процедуры пользователь не мог бы на неё вернуться.
  - **Важный нюанс по shadcn `Select` (Base UI)**: `<SelectValue />` без доп. пропа по умолчанию показывает **сырое значение** (`"PURCHASE"`), а не текст выбранного пункта (`"Закупка"`) — и это сохраняется даже после выбора другого пункта, если не подсказать компоненту соответствие. Фикс: передать `items={{ PURCHASE: "Закупка", SALE: "Продажа" }}` в `<Select items={...}>` — тогда `<Select.Value>` сам подставляет `label` вместо raw value. Подтверждено чтением `node_modules/@base-ui/react/select/root/SelectRoot.d.ts`. Если где-то ещё в проекте использовали `<SelectValue />` без `items` на `Select` — велика вероятность той же проблемы.
  - **Важный layout-баг, пофикшен в общем компоненте**: `components/ui/sidebar.tsx` → `SidebarInset` (`<main>`) имел классы `flex w-full flex-1 flex-col` без `min-w-0`. Классическая ловушка флексбокса: широкий контент внутри (в данном случае — таблица спецификации с горизонтальным скроллом) не сжимался, а раздувал `<main>` шире вьюпорта, из-за чего скроллилась вся страница целиком (включая сайдбар), а не сам `div[data-slot=table-container]` со своим `overflow-x-auto`. Добавлен `min-w-0` в class-list `SidebarInset` — теперь широкие таблицы/контент скроллятся только внутри себя. Это общий фикс уровня layout, будет действовать на все страницы дашборда, не только на спецификацию.
  - При браузерном тестировании: клики по «сырым» screenshot-координатам иногда промахивались (масштаб скриншота 800×450 не совпадает с реальным вьюпортом 1280×720) — клики через `ref` от `read_page`/`find` или через `document.querySelector(...).click()` в `javascript_tool` были надёжнее. Также подтверждено: `mcp__Claude_Browser__computer{action:"scroll"}` может подвисать (таймаут) на этой странице — вместо него скроллить внутренний контейнер таблицы через `element.scrollLeft = element.scrollWidth` в `javascript_tool`.
  - Создан `etp/.claude/launch.json` для превью через `mcp__Claude_Browser__preview_start`. На машине уже был запущен `next dev` на порту 3000 (отдельный процесс, PID виден через `lsof -i :3000`) — итоговый `launch.json` просто подключается к нему через `"url": "http://localhost:3000"`, а не запускает второй сервер.
  - **Важно:** основной рабочий каталог этой Claude-сессии — `specification` (repo `specification-anna`), не `etp`. Пользователь явно запретил трогать `specification-anna` вообще, даже локально/нетрекаемыми файлами. Один раз по ошибке был создан `specification/.claude/launch.json` для превью — сразу удалён после замечания пользователя. Для превью `etp` использовать `mcp__Claude_Browser__preview_start({url: "http://localhost:3000"})` напрямую — это не требует `launch.json` вообще и ничего не пишет в `specification`.
- **Левое меню (сайдбар) переделано под макет пользователя** (`components/nav-main.tsx`), полностью заменив прежний список «Дашборд/Процедуры/Инбокс»:
  - Верхняя группа без подписи: Главная (`/dashboard`), Мои процедуры (`/procedures`), Участие в процедурах (`/participation`), Приглашения (`/invitations`), Заявки из каталога (`/catalog-requests`), Настройки (кнопка, открывает `Dialog`-заглушку «Скоро здесь появятся настройки компании и аккаунта» — пользователь сказал, что пришлёт готовый компонент настроек отдельно, реальную форму пока не делали).
  - Группа «Сервисы» (`SidebarGroupLabel`): Закупки (`/marketplace/purchases`), Продажи (`/marketplace/sales`), Каталог (`/marketplace/catalog`) — по словам пользователя, это витрины **всех** закупок/продаж/товаров **всех** компаний площадки (не только своей). Сейчас все три — страницы-заглушки с `requireCompany()`.
  - `/dashboard` теперь «Главная» — карточка профиля компании (название, ИНН, описание) + заглушка «Каталог товаров и услуг» (per user: это будет каталог своей компании). Список процедур компании переехал на `/procedures` («Мои процедуры»), там же кнопка «Создать процедуру».
  - `/participation`, `/invitations`, `/catalog-requests` — страницы-заглушки, реальной логики нет. `/catalog-requests` — две карточки-заглушки: обращения по моим товарам / мои обращения по чужим товарам.
  - **Грабли с активным пунктом меню:** изначально «Участие в процедурах» повесили на `/procedures/participation` (вложенно под процедуры) — из-за `pathname.startsWith(url + "/")` в подсветке активного пункта оба пункта подсвечивались одновременно на этой странице. Исправлено переносом на отдельный верхнеуровневый роут `/participation` — если в будущем добавлять новые пункты меню, не вкладывать их под уже существующий пункт-путь без крайней необходимости, либо усложнять логику `isItemActive`.
  - Старый пункт «Инбокс» (`/inbox`, мессенджер) из меню убран — в макете пользователя его нет. Страница `/inbox` физически осталась в коде (не удалена, просто не залинкована), пригодится когда дойдём до мессенджера по плану.

**Дальше по плану:** пользователь ушёл продумывать дизайн-макеты дальше, следующий шаг обсудим отдельно. Мессенджер и ИИ-фичи — после этого.

## Важные технические решения (не переспрашивать, если не сказано иное)

- **Prisma зафиксирован на 6.19.3**, не 7.x — Prisma 7 убрал `url = env("DATABASE_URL")` из schema.prisma в пользу `prisma.config.ts` + driver adapters, это сломало бы готовую схему пользователя.
- Next.js 16: файл `middleware.ts` переименован в **`proxy.ts`**, экспортируемая функция должна называться `proxy`, не `middleware`.
- shadcn `Button` в этой установке использует `render` проп от Base UI (не `asChild` от Radix): `<Button render={<Link href="/x" />}>text</Button>`. Если `render` рендерит НЕ `<button>` (например, `<Link>`), обязательно добавляй `nativeButton={false}`, иначе консольный warning.
- Дашборд живёт на `/dashboard`, не на `/` — потому что `(marketing)` и `(platform)` route-группы не могут обе резолвиться в корень.
- `Prisma.User.id` = id пользователя Supabase Auth (без отдельной таблицы-связки) — так RLS-политики смогут опираться на `auth.uid()` напрямую.
- Дропдауны (`DropdownMenuLabel` и другие части) обязательно должны быть внутри `<DropdownMenuGroup>` — иначе краш "Base UI: MenuGroupContext is missing". Уже словили и пофиксили один раз в `nav-user.tsx`.
- Шрифт: чинили серьёзный баг — весь текст (включая латиницу) рендерился serif-шрифтом. Причина — циклическая ссылка `--font-sans: var(--font-sans)` в `globals.css` (должно быть `var(--font-inter)`) + отсутствующий cyrillic-сабсет у шрифта. Если в новом shadcn/create-next-app скэффолде снова увидишь нечитаемый/неправильный шрифт — проверяй именно этот паттерн.
- `ref_N` из `read_page`/`find` в браузерном тестировании этого приложения быстро протухают (особенно вокруг дропдаунов/после навигации) — надёжнее кликать через `javascript_tool` (`document.querySelector(...).click()`), чем по устаревшему `ref` или координатам.

## Окружение

На этой машине изначально не было Node/pnpm/brew/nvm в PATH инструмента Bash. Node 24 LTS установлен вручную в `~/.local/node` (тарбол с nodejs.org, без sudo), pnpm активирован через corepack. **Перед любой командой с node/pnpm/prisma** нужно:
```bash
export PATH="$HOME/.local/node/bin:$PATH"
```
Это не сохраняется между отдельными вызовами Bash.

## Supabase — реальный проект подключён

`.env` (в .gitignore, не в репозитории) содержит реальные `DATABASE_URL` (пулер, порт 6543, `pgbouncer=true`), `DIRECT_URL` (сессионный пулер, порт 5432 — нужен для миграций, pgbouncer transaction-mode их не умеет), `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` (новый формат `sb_publishable_...`), `SUPABASE_SERVICE_ROLE_KEY` (`sb_secret_...`). `prisma/schema.prisma` datasource содержит и `url`, и `directUrl`.

**"Confirm email" сейчас отключён** в настройках Supabase Auth (тестовые аккаунты сразу получают сессию) — обязательно включить обратно перед реальным запуском.

В базе остались тестовые пользователи/компании от проверки флоу — можно игнорировать или почистить.

## Git, GitHub, Vercel

- Репозиторий: **github.com/akmaevtat-lang/etp** (публичный). Прод: **https://etp-one.vercel.app**. Автодеплой на пуш в `main` уже подключён (Vercel GitHub App с доступом к репо).
- `gh` CLI стоит локально в `~/.local/gh/bin` (без sudo), уже залогинен как `akmaevtat-lang`. Vercel CLI запускается через `npx vercel`, залогинен как `akmaevtat-8516`.
- **Жёсткое правило: никогда не запускать `git config`** — ни глобально, ни локально, даже если пользователь явно просит и разрешает. Это осознанная политика безопасности агента, не техническое ограничение. Имя/email коммитов пользователь настраивает сам через Terminal (он не разработчик — давай точные copy-paste команды и объясняй, как открыть Terminal).
- Раз `gh auth login` не настраивает credential helper для обычного `git push`, а `gh auth setup-git` сам трогает git config (тоже под запретом) — пуш делается через одноразовый URL с токеном, без изменения конфига:
  ```bash
  git push "https://x-access-token:$(gh auth token)@github.com/akmaevtat-lang/etp.git" main
  ```
- В `pnpm-workspace.yaml` однажды остался буквальный плейсхолдер `'@prisma/client': set this to true or false` вместо `true` — локально не мешало (клиент уже был сгенерирован), но ломало билд на Vercel (чистая установка блокирует postinstall-скрипт `@prisma/client`, который запускает `prisma generate`). Если снова увидишь ошибку `Module '"@prisma/client"' has no exported member 'PrismaClient'` при деплое — сначала проверь этот файл на такие незаполненные плейсхолдеры.

## Работа в разных "Проектах" Claude

Пользователь завёл отдельный Claude Project «ETP X» для дальнейшей работы над этим кодом — отдельно от «Specification», где велась разработка изначально. Память ассистента (`.claude`-memory) привязана к проекту и **не** переносится между ними. Этот файл (`AI_HANDOFF.md`) — единственный переносимый источник контекста, держи его в актуальном состоянии при значимых изменениях.
